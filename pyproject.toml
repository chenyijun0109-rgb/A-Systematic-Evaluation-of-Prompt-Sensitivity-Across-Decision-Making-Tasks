[project]
name = "project"
version = "0.1.0"
description = "Evaluate prompt sensitivity in LLM cognitive decision-making tasks"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy>=2.2.6",
    "openpyxl>=3.0.10",
    "scipy>=1.14.1",
]
